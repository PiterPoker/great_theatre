@model great_theatre.Models.Entities.Perfomance

@{
    ViewBag.Title = "AddPerfomence";
    Layout = "~/Views/Admin/_Layout.cshtml";
}
<div id="page-wrapper">
    <div class="graphs">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="col-md-8 inbox_right">
                <div class="Compose-Message">
                    <div class="panel panel-default">
                        <div class="panel-heading">
                            Доавление новой постановки
                        </div>

                        <div class="panel-body">

                            
                                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                            
                            <hr>
                            <div class="form-group">
                                    @Html.EditorFor(model => model.Name, new {htmlAttributes = new { placeholder="Введите новую постановку", @class = "form-control1 control3"}})
                                    @Html.ValidationMessageFor(model => model.Name, "", new {@class = "text-danger"})
                            </div>
                            <hr>
                                    <input type="submit" value="Create" class="btn btn-warning btn-warng1" />
                        </div>
                    </div>
                </div>
            </div>
            <div class="clearfix"> </div>
        }
        <div>
            @Html.ActionLink("Главная", "Index", null, new { @class = "btn btn-warning btn-warng1" })
        </div>
    </div>
</div>